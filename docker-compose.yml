version: '3.8'

services:
  omnibug:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: omnibug-platform
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000
      - FRONTEND_ORIGIN=http://localhost:5173
    restart: unless-stopped
